"""
4chan Harvester – Import 4chan data into the Ashchan database.

Supports:
  • Harvesting entire boards (catalog → all threads)
  • Harvesting individual threads
  • Harvesting a single board's catalog (OPs only)
  • Downloading and storing images in MinIO/S3
  • Resumable operation via deduplication
"""
